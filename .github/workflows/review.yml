name: Review

on:
  workflow_call:

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [14.x]
    environment:
      name: testo
    steps:
      - uses: actions/checkout@v1
      - name: Test
        run: echo HOLA
  deployprod:
    build: deploydev
    environment:
      name: testo
      url: https://jobsandenvironments.azurewebsites.net/
    runs-on: ubuntu-latest
    steps:
      - name: Test
        run: echo HOLA