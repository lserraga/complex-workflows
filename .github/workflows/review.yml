name: Review

on:
  workflow_call:

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [14.x]
    environment:
      name: testo
    steps:
      - uses: actions/checkout@v1
      - name: Test
        run: echo HOLA